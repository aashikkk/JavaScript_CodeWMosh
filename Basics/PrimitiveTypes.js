/* 
Primitive data Types
1. String
2. Number
3. Boolean
4. undefined
5. null
*/

let name = 'Aash'   // String literal
let age = 25;   // Number literal
let isApproved = true   // Boolean literal , true or false

// let firstName; // undefined default value
let firstName = undefined; // explicitly defining value, not common
// undefined also a value and type

let selectedColor = null; 
// Ex: We use this we we click button that will change the color to something.
// and then if click again it should restore to the previous color

console.log(typeof isApproved);
console.log(typeof age);

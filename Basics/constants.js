const interestRate = 0.3
// interestRate = 1
//TypeError: Assignment to constant variable.
console.log(interestRate);

// Here variable is constant, so we cannot alter it.
// We use this variable, when we doesnt want to change at all.